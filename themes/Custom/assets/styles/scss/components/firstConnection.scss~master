#FirstConnection{
  #btnFirst{
    font-size: 20px;
  }
  #head_content
  {

    h1{
      font-size: 36px;
      @media #{$w1} {
        font-size: 2rem;
      }
    }
    #secondTitle{
      font-size: 1.7rem;
    }
    h2{
      @media #{$w1} {
        font-size: 1.5rem;
      }
    }
  }
  #presentPhrase{
    font-weight: 600;
    font-size: 32px;
  }

  #container_label{
    display: flex;
    justify-content: center;
    *{

    }
    a{
      color : rgb(41,41,41);
      border-bottom: solid 1px rgba(41,41,41,0.5); 

      &:hover {
        text-decoration: none;
        border-bottom: solid 3px rgb(41,41,41); 
      }
    }
    input{
      height: 20px;
      width: 20px;
      margin-right: 10px;
    }
  }
  #containerError{
    display: none;
  }
  #containerErrorRule, #containerErrorWatch{
    span{
        display: none; 
    }
  }

  #visuelContratuel 
  {
    opacity: 0.7;
  }
  .containerRadioWatch{
    position: relative;
    overflow: hidden;
    text-align: center;
    label{
      display: block;

      cursor: pointer;
      text-align: center;
      background: white;
      border-radius: 3%;
      padding: 15px;
      @include transition($ts);

      .fa {
        vertical-align: middle;
        width: 16px;
        text-align: left; 
        margin-top: -2px;
      }

      span{
        font-family: HelveticaNeueCondensed;
        font-size: 18px;
        line-height: 20px;
      }

      img{
        @include transition($ts);
        opacity: 0.7;
        display: block;
        max-width: 100%;
        margin-bottom: 30px;
        @include transform(scale(0.9));
      }

      .fa-check-square-o{
        display: none;
      }
      .fa-square-o{
        display: inline-block;
      }
    }

    input{
      position: absolute;
      left: -6000px;

      &:checked+label{

          opacity: 1;
          img{
            opacity: 1;
            @include transform(scale(1.05));
          }
          .fa-check-square-o{ 
            display: inline-block;
          }
          .fa-square-o{
            display: none;
          }
      } 
    } 

    &:hover {
      img{
        opacity: 1;
        @include transform(scale(1.1));
      } 
    }
  }
}
