
#blueHeader{
  background-color: #336dac;
  #content_logo{
    margin-left: 60px;
    width: 120px;
    height: $headerHeight / 2;
    background-color: #014a97;
    position: relative;
    img{
      width: 100%;
      position: absolute;
      bottom: 0;
      left: 0;
    }
  }
}
#contain-nav{
  margin-bottom: 60px;
  padding: 15px;
  min-height: $headerHeight;
  @include hardware-accelerated();
  @media #{$w2} {
    background-size: cover;
  }
  @media #{$w5} {
    height:  $headerHeight * 1.4;
  }
  @include boxshadow( inset 0px -13px 30px 0px rgba(0,0,0,0.50) );
  position: relative;

  .background-container {
    position: absolute;
    z-index: 0;
    left: 0;
    right: 0;
    bottom: 0;
    top: 0;
    overflow: hidden;

    .background {
      position: absolute;
      z-index: 0;
      left: -40px;
      right: -40px;
      bottom: -20px;
      top: -20px;
      background-image: url('/assets/images/header_1920_original.jpg');
      background-size: 100% auto;
      background-position: 50% 50%;
    }
  }

  .yellowFilter
  {
    opacity: 0.5;
    background-color: $yellow;
    width: 100%;
    height: 100%;
    left: 0;
    top: 0;
    position: absolute;
    z-index: 1;

    animation: yellowFilter-animation 4s infinite;
    -webkit-animation: yellowFilter-animation 4s infinite;

    @keyframes yellowFilter-animation {
      0% { opacity: 0.4; }
      50% { opacity: 0.5; }
      100% { opacity: 0.4; }
    }
    @-webkit-keyframes yellowFilter-animation {
      0% { opacity: 0.4; }
      50% { opacity: 0.5; }
      100% { opacity: 0.4; }
    }
  }


  .container{
    height: 100%;
    z-index: 2;
    .row{
        height: 100%;
    }
  }
  #nav-logo{
    position: relative;
    min-height: $headerHeight;
    @media #{$w2}{

    }

    .logo {
      position: absolute;
      display: block;
      bottom: -50px;
      left: 15px;
      right: 15px;
      background: white;
      border-radius: 100%;
      z-index: 0;

      &:before {
        content: '';
        display: block;
        position: relative;
        padding-top: 100%;
      }

      img {
        position: absolute;
        z-index: 1;
        left: 5%;
        width: 90%;
        height: auto;
        top: 50%;
        @include transform(translateY(-50%));
      }
    }
  }
  #nav-menu{
      display: flex;
      .content_header_title{
          margin: auto 0;
          @media #{$w2} {
            margin: auto auto;
            text-align: center;
          }

      }
      h1, h2{
        color: white;
      }
      h1{
          text-transform: uppercase;
          font-family: 'HelveticaNeueCondensed';
          margin-bottom: 0;
          font-weight: bold;
          font-size: 4.2rem;
          @media #{$w1} {
            font-size: 2rem;
          }
      },
      h2{
        font-family: 'HelveticaNeueCondensed';
        font-size: 2.9rem;
        @media #{$w1} {
          font-size: 1.5rem;
        }
      }
  }
}
